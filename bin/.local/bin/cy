#!/bin/bash
# cy: Claude headless, no API key (uses OAuth)
# Runs in whatever directory you launch it from -- no implicit cd.
unset ANTHROPIC_API_KEY
unset CLAUDECODE
export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
# Claude in Chrome on for every run (each session gets its own tab group);
# pass --no-chrome to opt out.
chrome=(--chrome)
for a in "$@"; do
  [ "$a" = --no-chrome ] && chrome=()
done
exec ~/.local/bin/claude --dangerously-skip-permissions "${chrome[@]}" "$@"
